function solution(s){
    const string = s.toLowerCase().split('');
    const p = string.filter((keyword) => keyword === 'p').length;
    const y = string.filter((keyword) => keyword === 'y').length;
    
    // [실행] 버튼을 누르면 출력 값을 볼 수 있습니다.
    console.log('Hello Javascript')

    return answer = p === y;
}